import { IJar } from "../Components/interfaces";

const jarOne: IJar = {
    id: 10,
    ballsId: [2, 1]
}

const jarTwo: IJar = {
    id: 10,
    ballsId: [3, 4]
}

const jarsSet = [jarOne, jarTwo];

export default jarsSet;